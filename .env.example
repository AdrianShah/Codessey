# Local dev — each developer uses their own free AI Studio key
GEMINI_API_KEY=your_personal_key_here

# Demo rehearsal only — shared team key (do not use during parallel integration testing)
# GEMINI_API_KEY=team_demo_key_here
